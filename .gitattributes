# Shell scripts must keep LF endings so they stay executable on Linux even when
# the repository is checked out on Windows.
*.sh text eol=lf
# PowerShell scripts are Windows only.
*.ps1 text eol=crlf
